# Deploying apps to AKS using YAML manifests

This directory contains YAML manifest files for deploying a sample application to Azure Kubernetes Service (AKS).

To deploy the yaml files:

```sh
kubectl apply -f .
```